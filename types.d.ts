/*
 * The type declarations are generated from the JSDoc in `lib/` and live in
 * `types/`. This file re-exports the entry point so that the `types` field and
 * any direct reference to `enhanced-resolve/types.d.ts` keep resolving.
 */

import resolve = require("./types/index");

export = resolve;
